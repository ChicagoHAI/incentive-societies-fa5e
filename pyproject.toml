[project]
name = "incentive-societies-research"
version = "0.1.0"
description = "Research workspace for Incentive-Compatible Societies experiments"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
